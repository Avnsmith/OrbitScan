version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: orbitscan-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: orbitscan
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7-alpine
    container_name: orbitscan-redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  pgdata:
